{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hazinginfo.org/schemas/validation.schema.json",
  "title": "ai/extract_vN/validation.json (v2 -- v3.0 pipeline)",
  "description": "validate.py output: schema-conformance result only. There is no anchoring or tier assignment -- extraction_confidence and flags[] (carried in incidents.json itself, see jobs/04-extract/schema.json) are what a reviewer actually uses instead.",
  "type": "object",
  "properties": {
    "schema_version": {"const": 2},
    "valid": {"type": "boolean"},
    "schema_errors": {
      "type": "array",
      "items": {"type": "string"}
    }
  },
  "required": ["schema_version", "valid", "schema_errors"],
  "additionalProperties": false
}
