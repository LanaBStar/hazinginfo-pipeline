{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hazinginfo.org/schemas/validation.schema.json",
  "title": "ai/extract_vN/validation.json (v2 -- v3.0 pipeline)",
  "description": "validate.py output: schema-conformance result only. Per IMPLEMENTATION_PLAN.md §7 (04-extract). v3.0 drops anchoring/tier assignment entirely -- extraction_confidence and flags[] (carried in incidents.json itself, see jobs/04-extract/schema.json) replace what this file used to compute.",
  "type": "object",
  "properties": {
    "schema_version": {"const": 2},
    "valid": {"type": "boolean"},
    "schema_errors": {
      "type": "array",
      "items": {"type": "string"}
    }
  },
  "required": ["schema_version", "valid", "schema_errors"],
  "additionalProperties": false
}
